# Redux-exemple

instal avec `npm install`

lancer avec `npm start`

# Changer le composant

Dans le fichier App.js, changer l'import de GrandRectangle :
- "./components-react/GrandRectangle" **par défaut**
- "./components-probleme/GrandRectangle" **react mais problematique**
- "./components-redux/GrandRectangle" **solution avec redux**
